rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY TEST MODE - ALLOWS ALL ACCESS
    // This is to diagnose the PERMISSION_DENIED issue
    // DO NOT USE IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
