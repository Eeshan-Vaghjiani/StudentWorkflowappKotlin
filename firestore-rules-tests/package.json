{
    "name": "firestore-rules-tests",
    "version": "1.0.0",
    "description": "Firebase Firestore Rules Testing",
    "scripts": {
        "test": "jest --testEnvironment=node",
        "test:watch": "jest --watch --testEnvironment=node"
    },
    "devDependencies": {
        "@firebase/rules-unit-testing": "^3.0.0",
        "jest": "^29.7.0",
        "@types/jest": "^29.5.0"
    }
}